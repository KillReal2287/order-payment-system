<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                            https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="003-create-tasks-table" author="bronnikov">
        <createTable tableName="tasks">
            <column name="id" type="UUID" defaultValueComputed="uuidv7()">
                <constraints nullable="false" primaryKey="true" primaryKeyName="pk_tasks"/>
            </column>
            <column name="order_id" type="UUID">
                <constraints nullable="false"
                             foreignKeyName="fk_tasks_order"
                             references="orders(id)"/>
            </column>
            <column name="status" type="VARCHAR(64)" defaultValue="NEW">
                <constraints nullable="false"/>
            </column>
            <column name="attempts" type="INTEGER" defaultValueNumeric="0">
                <constraints nullable="false"/>
            </column>
            <column name="next_attempt_at" type="TIMESTAMP WITH TIME ZONE"/>
            <column name="created_at" type="TIMESTAMP WITH TIME ZONE">
                <constraints nullable="false"/>
            </column>
            <column name="updated_at" type="TIMESTAMP WITH TIME ZONE">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <createIndex indexName="idx_tasks_status_next_attempt_at" tableName="tasks">
            <column name="status"/>
            <column name="next_attempt_at"/>
        </createIndex>

        <createIndex indexName="idx_tasks_order_id" tableName="tasks">
            <column name="order_id"/>
        </createIndex>
    </changeSet>

</databaseChangeLog>
